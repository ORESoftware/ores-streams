# Conformance

This directory is the repository-local conformance boundary.

Put shared stream behavioral fixtures, negative cases, compatibility matrices, and executable checks here. Conformance evidence tests contracts and compatibility; it must not become a second authority.

Existing mature tests do not need to move solely for layout. New cross-version/runtime evidence should grow here and may call existing test harnesses.
